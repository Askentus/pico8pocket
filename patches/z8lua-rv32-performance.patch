diff --git a/lvm.c b/lvm.c
index 7943ea0..39da43a 100644
--- a/lvm.c
+++ b/lvm.c
@@ -575,16 +575,9 @@ void luaV_finishOp (lua_State *L) {
 #define vmcase(l,b)	case l: {b}  break;
 #define vmcasenb(l,b)	case l: {b}		/* nb = no break */
 
-//turn off optimizations for this method.
-//OP_FORLOOP doesn't correctly detect wrapping in certain cases if optimization is on and aggressive
-#if __clang__
-[[clang::optnone]]
+// Pico8Pocket compiles with -fwrapv, which preserves the signed wrapping
+// relied upon by OP_FORLOOP and lets the hot VM dispatch loop be optimized.
 void luaV_execute (lua_State *L) {
-#elif __GNUC__
-void __attribute__((optimize("O0"))) luaV_execute (lua_State *L) {
-#else
-void luaV_execute (lua_State *L) {
-#endif
   CallInfo *ci = L->ci;
   LClosure *cl;
   TValue *k;
diff --git a/lpico8lib.c b/lpico8lib.c
index 84aa1e7..9ff936b 100644
--- a/lpico8lib.c
+++ b/lpico8lib.c
@@ -61,7 +61,12 @@ static lua_Number sin_helper(lua_Number x) {
     // We use a lookup table of sin(x)-4x generated by PICO-8 to ensure
     // that we get the exact same results.
     auto a = ((x.bits() & 0x4000 ? ~x : x).bits() & 0x3fff) + 2;
-    auto ret = lua_Number::frombits((a >> 2 << 4) + sintable[a >> 2]);
+    // Rounding the last two bits can produce table index 4096 at the
+    // quarter-turn boundary.  The correction term wraps to entry zero there;
+    // indexing 4096 was an out-of-bounds read and became unstable once the VM
+    // was optimized.
+    auto ret = lua_Number::frombits((a >> 2 << 4) +
+                                    sintable[(a >> 2) & 0xfff]);
     return x.bits() & 0x8000 ? ret : -ret;
 }
 
diff --git a/llex.c b/llex.c
index c90a47e..7a033a0 100644
--- a/llex.c
+++ b/llex.c
@@ -435,7 +435,7 @@ static int llex (LexState *ls, SemInfo *seminfo) {
     switch (ls->current) {
       case '\n': case '\r': {  /* line breaks */
         inclinenumber(ls);
-        if (ls->emiteol) { ls->emiteol = 0; return TK_EOL; }
+        if (ls->emiteol) return TK_EOL;
         break;
       }
       case ' ': case '\f': case '\t': case '\v': {  /* spaces */
@@ -444,7 +444,7 @@ static int llex (LexState *ls, SemInfo *seminfo) {
       }
       case '?': {  /* '?' (shorthand print) */
         next(ls);
-        ls->emiteol = 1;
+        ++ls->emiteol;
         return TK_PRINT;
       }
       case '-': {  /* '-' or '-=' or '--' (comment) */
diff --git a/lparser.c b/lparser.c
index 5f771d4..7483fbc 100644
--- a/lparser.c
+++ b/lparser.c
@@ -605,6 +605,20 @@ static int block_follow (LexState *ls, int withuntil) {
 }
 
 
+/*
+** A single source newline can close several nested PICO-8 short statements.
+** Keep the synthesized EOL token in place until every nested consumer has
+** closed; the old boolean flag let the innermost short IF consume the EOL and
+** accidentally pulled the outer IF over all following source lines.
+*/
+static void close_short_line (LexState *ls) {
+  lua_assert(ls->emiteol > 0);
+  --ls->emiteol;
+  if (ls->emiteol == 0 && ls->t.token == TK_EOL)
+    luaX_next(ls);
+}
+
+
 static void statlist (LexState *ls) {
   /* statlist -> { stat [`;'] } */
   while (!block_follow(ls, 1)) {
@@ -1297,7 +1311,7 @@ static void whilestat (LexState *ls, int line) {
   int short_while = ls->t.token != TK_DO && ls->t.token != TK_EOS
                  && ls->braces == 0 && line == ls->linenumber;
   if (short_while)
-    ls->emiteol = 1;
+    ++ls->emiteol;
   else
     checknext(ls, TK_DO);
   block(ls);
@@ -1305,9 +1319,9 @@ static void whilestat (LexState *ls, int line) {
   if (!short_while)
     check_match(ls, TK_END, TK_WHILE, line);
   else if (ls->t.token == TK_EOL || ls->t.token == TK_EOS)
-    luaX_next(ls);  /* eat EOL or EOS */
+    close_short_line(ls);
   else if (block_follow(ls, 1))
-    ls->emiteol = 0;  /* close the short WHILE */
+    --ls->emiteol;  /* close the short WHILE */
   else
     check_match(ls, TK_EOL, TK_WHILE, line);  /* we expected EOL */
   leaveblock(fs);
@@ -1451,7 +1465,7 @@ static int test_then_block (LexState *ls, int *escapelist) {
   short_if &= ls->t.token != TK_THEN && ls->t.token != TK_DO && ls->t.token != TK_EOS
            && ls->braces <= 0 && line == ls->linenumber;
   if (short_if)
-    ls->emiteol = 1;
+    ++ls->emiteol;
   else {
     /* PICO-8 allows 'do' as an alternative to 'then' in if statements */
     if (!testnext(ls, TK_THEN) && !testnext(ls, TK_DO))
@@ -1497,9 +1511,9 @@ static void ifstat (LexState *ls, int line) {
     check_match(ls, TK_END, TK_IF, line);
   else {
     if (ls->t.token == TK_EOL || ls->t.token == TK_EOS)
-      luaX_next(ls);  /* eat EOL or EOS */
+      close_short_line(ls);
     else if (block_follow(ls, 1))
-      ls->emiteol = 0;  /* close the short IF */
+      --ls->emiteol;  /* close the short IF */
     else
       check_match(ls, TK_EOL, TK_IF, line);  /* we expected EOL */
   }
@@ -1639,7 +1653,8 @@ static void shortprint (LexState *ls, expdesc *f) {
 
   /* PICO-8: removed check for EOL so it can be used as an expression */
   /* if (!testnext(ls, TK_EOS)) check_match(ls, TK_EOL, '?', line); */
-  testnext(ls, TK_EOL);
+  if (ls->t.token == TK_EOL || ls->t.token == TK_EOS)
+    close_short_line(ls);
 
   int base, nparams;
   lua_assert(f->k == VNONRELOC);
diff --git a/fix32.h b/fix32.h
--- a/fix32.h
+++ b/fix32.h
@@ -117,6 +117,11 @@ struct fix32
 
     fix32 operator *(fix32 x) const
     {
+        if ((x.m_bits & 0xffff) == 0) {
+            int32_t multiplier = x.m_bits >> 16;
+            return frombits(int32_t(uint32_t(m_bits) *
+                                    uint32_t(multiplier)));
+        }
         return frombits(int64_t(m_bits) * x.m_bits >> 16);
     }
 
@@ -126,6 +131,19 @@ struct fix32
         if (x.m_bits == 0x1'0000)
             return *this;
 
+        // Dividing Q16.16 by an exact integer does not need the generic
+        // 64-bit quotient: (a << 16) / (n << 16) is exactly a / n.  RV32M
+        // has a hardware 32-bit divide, while the generic expression calls
+        // the much slower __divdi3 helper on Pocket.
+        if ((x.m_bits & 0xffff) == 0) {
+            int32_t divisor = x.m_bits >> 16;
+            if (divisor) {
+                if (m_bits == int32_t(0x8000'0000u) && divisor == -1)
+                    return frombits(0x7fff'ffff);
+                return frombits(m_bits / divisor);
+            }
+        }
+
         if (x.m_bits)
         {
             using std::abs;
